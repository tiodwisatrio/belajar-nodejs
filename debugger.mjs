function sayHello(name) {
    debugger
    return `Hello ${name}`;
}

const name = "Tio";
console.log(sayHello(name));
// Stepping
// cont, c: Continue execution
// next, n: Step next
// step, s: Step in
// out, o: Step out
// pause: Pause running code (like pause button in Developer Tools)

// Dokumentasi : https://nodejs.org/api/debugger.html